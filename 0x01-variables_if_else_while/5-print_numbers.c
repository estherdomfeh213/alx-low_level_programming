#include <stdio.h>


/**
 * main - A program that prints the alphabet in lowercase and upper case
 *
 * Return: 0(Success).
 */

int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
		printf("%d", i);
	putchar('\n');
	return (0);

}

